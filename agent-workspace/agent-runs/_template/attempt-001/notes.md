# Attempt 001

## Command

```bash
pnpm bench agent-workspace/agent-runs/<agent-or-model-slug>/<level-id>/attempt-001/action.json --bundle agent-workspace/agent-runs/<agent-or-model-slug>/<level-id>/attempt-001/run.json
```

## Result

- success:
- terminalState:
- firstSuccessFrame:
- terminalFrame:
- finalBall:
- runPath:
- attemptTrace: agent-workspace/agent-runs/<agent-or-model-slug>/<level-id>/attempt-001/run.json

## Observations

Record what happened. Use the browser Trace mode for visual inspection instead of reading the whole trace JSON.

## Next Idea

If this failed, describe the next change before creating `attempt-002`.
